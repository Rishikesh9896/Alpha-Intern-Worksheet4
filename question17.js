// Define the number for which we want to print the multiplication table
var number = 19;

// Loop to print the multiplication table of the given number
for (var i = 1; i <= 10; i++) {
    var result = number * i;
    console.log(number + " x " + i + " = " + result);
}
